name: Temporary inspect new screenshots

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/inspect-new-screenshots.yml"

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: OCR screenshots for module identification
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v tesseract >/dev/null 2>&1; then
            sudo apt-get update -qq
            sudo apt-get install -y -qq tesseract-ocr
          fi
          for f in \
            PixPin_2026-09-14_09-10-56.png \
            PixPin_2026-09-14_09-16-36.png; do
            echo "===== ${f} ====="
            tesseract "images/screenshots/${f}" stdout --psm 11 2>/dev/null | head -n 360 || true
          done
